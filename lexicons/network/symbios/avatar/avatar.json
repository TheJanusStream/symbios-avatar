{
  "lexicon": 1,
  "id": "network.symbios.avatar.avatar",
  "defs": {
    "main": {
      "type": "record",
      "description": "One parametric avatar in an identity's wardrobe. The record is the source of truth: geometry is derived from these parameters on demand rather than stored, so an avatar is owned by the identity rather than by any application that renders it. Several avatars may exist; network.symbios.avatar.profile names the default.",
      "key": "tid",
      "record": {
        "type": "object",
        "required": [
          "name",
          "archetype",
          "createdAt"
        ],
        "properties": {
          "name": {
            "type": "string",
            "description": "Display name, as shown in a wardrobe.",
            "maxLength": 640,
            "maxGraphemes": 64
          },
          "archetype": {
            "type": "union",
            "description": "The body this avatar describes. Open: readers that do not recognise a variant should say so rather than substitute another body.",
            "refs": [
              "network.symbios.avatar.defs#humanoid",
              "network.symbios.avatar.defs#quadruped"
            ]
          },
          "composites": {
            "type": "ref",
            "ref": "network.symbios.avatar.defs#composites",
            "description": "How the body is described at the level a person is described at: femininity, mass, body fat and age, which fan out through formulas to many quantities at once. The archetype's own axes apply as offsets on top. Absent means the neutral description, which is the body an archetype builds on its own."
          },
          "seed": {
            "type": "integer",
            "description": "Seed of the last re-roll, kept so a look can be reproduced."
          },
          "locks": {
            "type": "integer",
            "description": "Bitmask of parameter categories a re-roll must leave alone. Bit 0 stature, 1 build, 2 frame, 3 proportions, 4 head, 5 colouring, 6 hair, 7 age. Bit 4 formerly meant every feature at once — head shape, complexion, hair and extremity size together — and was narrowed to head shape alone, with colouring, hair and age given bits of their own and extremity size moved into proportions; a lock set written before that change holds less than it meant to. Unknown bits must be preserved on write so a newer client's locks survive an older one editing the record.",
            "minimum": 0
          },
          "generator": {
            "type": "integer",
            "description": "Which generation of the re-roll produced this record's parameters. A seed reproduces a look only against the generator that drew it; each axis draws from its own named stream so adding an axis cannot disturb another, but a reader carrying a lower number should not assume it can rebuild the same body from the seed alone.",
            "minimum": 0,
            "default": 1
          },
          "createdAt": {
            "type": "string",
            "format": "datetime"
          },
          "skin": {
            "type": "ref",
            "ref": "network.symbios.avatar.defs#skin",
            "description": "The complexion painted onto the body. Absent means the default."
          },
          "eyes": {
            "type": "ref",
            "ref": "network.symbios.avatar.defs#eyes",
            "description": "How the eyes are shaped and set. Absent means the default."
          },
          "face": {
            "type": "ref",
            "ref": "network.symbios.avatar.defs#face",
            "description": "How prominent the nose, brow, mouth and ears are. Absent means the default."
          },
          "hair": {
            "type": "ref",
            "ref": "network.symbios.avatar.defs#hair",
            "description": "The hair on the head, in five follicle regions of two layers each. Absent means the default, which is a cropped head with full brows and no facial hair."
          },
          "outfit": {
            "type": "ref",
            "ref": "network.symbios.avatar.defs#outfit",
            "description": "What the avatar is wearing. Absent means the default."
          }
        }
      }
    }
  }
}
