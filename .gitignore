/target
# Workspace members keep their build output here too; ignore at any depth.
target/
**/*.rs.bk
/.claude
/.chainlink
# Hook-created .chainlink caches inside workspace member crates.
**/.chainlink/
Cargo.lock
CHANGELOG.md
symbios-avatar-primer.sh
symbios_avatar_primer_*
.mcp.json
/exports
# Render goldens are session-local by decision: bless before a change, check
# after, never commit. See docs/instruments.md.
/tests/golden
